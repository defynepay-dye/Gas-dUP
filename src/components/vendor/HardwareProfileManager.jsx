import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export default function HardwareProfileManager() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>HardwareProfileManager</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">HardwareProfileManager - Coming soon</p>
      </CardContent>
    </Card>
  );
}
