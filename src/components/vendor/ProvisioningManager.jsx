import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export default function ProvisioningManager() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>ProvisioningManager</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">ProvisioningManager - Coming soon</p>
      </CardContent>
    </Card>
  );
}
