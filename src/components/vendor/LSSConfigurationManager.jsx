import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export default function LSSConfigurationManager() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>LSSConfigurationManager</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">LSSConfigurationManager - Coming soon</p>
      </CardContent>
    </Card>
  );
}
