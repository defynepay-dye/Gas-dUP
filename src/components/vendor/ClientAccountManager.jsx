import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export default function ClientAccountManager() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>ClientAccountManager</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">ClientAccountManager - Coming soon</p>
      </CardContent>
    </Card>
  );
}
