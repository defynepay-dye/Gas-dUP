import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export default function SupportDashboard() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>SupportDashboard</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">SupportDashboard - Coming soon</p>
      </CardContent>
    </Card>
  );
}
